# Entrada do ETL: arquivos NDJSON fechados pela API .NET.
SPOOL_PATH=../spool
ETL_POLL_SECONDS=10

# ClickHouse local (infra/docker-compose.yml).
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DATABASE=alphractal
CLICKHOUSE_USER=alphractal_etl
CLICKHOUSE_PASSWORD=etl_dev_2026
CLICKHOUSE_SECURE=false

# Somente para o comando de backfill. Nunca coloque uma chave real neste arquivo.
ALCHEMY_API_KEY=
