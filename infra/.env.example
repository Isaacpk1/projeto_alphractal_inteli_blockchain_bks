# Copie para .env  ->  cp .env.example .env
# Vale só para o ambiente local. Não versione o .env.

CLICKHOUSE_DB=alphractal
CLICKHOUSE_USER=alphractal
CLICKHOUSE_PASSWORD=alphractal_dev

CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_NATIVE_PORT=9000

# Usuários de aplicação criados por clickhouse/initdb/005_users.sql
CLICKHOUSE_API_USER=alphractal_api
CLICKHOUSE_API_PASSWORD=api_dev_2026
CLICKHOUSE_ETL_USER=alphractal_etl
CLICKHOUSE_ETL_PASSWORD=etl_dev_2026
